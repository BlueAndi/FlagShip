slam_toolbox:
  ros__parameters:

    # TurtleBot-like world variant for the Zumo.
    # Changed from slam_toolbox_params.yaml:
    # - resolution: 0.05
    # - map_update_interval: 5.0
    # - minimum_travel_distance: 0.15
    # - minimum_travel_heading: 0.15
    # - scan_buffer_size: 40
    # - scan_buffer_maximum_scan_distance: 4.0
    # - link_scan_maximum_distance: 2.0
    # - loop_search_maximum_distance: 2.5

    # Plugin params
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: HuberLoss

    # ROS Parameters
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    use_map_saver: true
    mode: mapping #localization

    # if you'd like to immediately start continuing a map at a given pose
    # or at the dock, but they are mutually exclusive, if pose is given
    # will use pose
    #map_file_name: test_steve
    # map_start_pose: [0.0, 0.0, 0.0]
    #map_start_at_dock: true

    debug_logging: false
    throttle_scans: 1
    transform_publish_period: 0.02 #if 0 never publishes odometry
    map_update_interval: 5.0 # changed for the larger TurtleBot-like world
    resolution: 0.05 # changed from 0.025 to reduce memory use in the larger world
    restamp_tf: false
    min_laser_range: 0.02 # modified:  (default 0.0)
    max_laser_range: 4.0 # changed to match the Zumo lidar max range
    minimum_time_interval: 0.1 # process scans more frequently to reduce pose jumps while moving
    transform_timeout: 0.2
    tf_buffer_duration: 30.0
    stack_size_to_use: 40000000 #// program needs a larger stack size to serialize large maps
    enable_interactive_mode: true

    # General Parameters
    use_scan_matching: true
    use_scan_barycenter: false
    minimum_travel_distance: 0.15 # changed for a larger world to reduce over-updating
    minimum_travel_heading: 0.15 # changed for a larger world to reduce over-updating
    check_min_dist_and_heading_precisely: false
    scan_buffer_size: 40 # changed from 30 to keep more history for loop closure
    scan_buffer_maximum_scan_distance: 4.0 # changed to match the Zumo lidar max range
    link_match_minimum_response_fine: 0.1
    link_scan_maximum_distance: 2.0 # changed from 1.5 for wider loop-closure candidates
    loop_search_maximum_distance: 2.5 # changed from 1.4 for the larger environment
    do_loop_closing: true
    loop_match_minimum_chain_size: 10
    loop_match_maximum_variance_coarse: 3.0
    loop_match_minimum_response_coarse: 0.35
    loop_match_minimum_response_fine: 0.45

    # Correlation Parameters - Correlation Parameters
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Correlation Parameters - Loop Closure Parameters
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03

    # Scan Matcher Parameters
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0

    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true
    min_pass_through: 2
    occupancy_threshold: 0.1